# Review

<!-- List only what a generic reviewer (`/code-review`) cannot infer from the
     diff alone: this project's non-obvious invariants. Delete this comment. -->

## Invariants

- **{{The invariant.}}** {{Why it matters here — the failure mode if broken.}}
- **{{Invariant.}}** {{Why.}}

## Do not flag

{{Things that look wrong to a generic reviewer but are intentional here — save
a reviewer from re-litigating a settled choice. Reference `DECISIONS.md` when
the choice is recorded there.}}
